import React from "react";
import ProductCard from "../ProductCard/ProductCard";

const ProductList = () => {
  return (
    <section>
      <h1>Product List</h1>
    </section>
  );
};

export default ProductList;
